#!/bin/sh
# Inspect the complete staged snapshot, including force-added private paths.
set -eu
root=$(git rev-parse --show-toplevel)
cd "$root"
exec sh tools/run-privacy-guard.sh scan --scope staged --require-local
